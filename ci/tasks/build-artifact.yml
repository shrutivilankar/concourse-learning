---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/concourse:4.2.1
    tag: "latest"


inputs:
  - name: git-repo
  - name: version

outputs:
  - name: artifact-dir

run:
  path: git-repo/ci/tasks/build-artifact.sh
